version: '3.4'

volumes:
  nemushee_postgres_v2:
   external: true
  redis:
    driver: local
  sidekiq:
    driver: local
  storage:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /mnt/disk2t/nemushee
  elasticsearch:
    driver: local